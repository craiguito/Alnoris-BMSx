# Archive

This directory contains source code preserved for reference during the TwinCore and BatteryTwin refactor.

`desktop_cpp_legacy/` is the old Qt/C++ desktop shell. It was archived during the backend-first cleanup so the active repository presents TwinCore and BatteryTwin as the current architecture.

Archived code is not part of the normal Python test suite and should not be used for new backend work.
